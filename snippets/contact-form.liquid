<section id="page-contact" class="section">
  <div class="container">
    <div class="page_content">

      <div class="contact_content">
        <div class="contact_col">{{ content }}</div>
        <div class="contact_col">
          
          {%- form 'contact', class: 'contactForm' -%}
            {%- if form.errors -%}  
            <ul class="contactForm_errors">
              {%- for field in form.errors -%}
              <li>{{ field | replace: 'body', 'Message' | capitalize }} - {{ form.errors.messages[field] }}</li>
              {%- endfor -%}
            </ul>
            {%- endif -%}

            {%- if form.posted_successfully? -%}
            <div class="contactForm_success">{{ 'customer.contact.post_success' | t }}</div>
            {%- endif -%}

            <div class="formGroup">
              <label class="label" for="contact_name">{{ 'contact.form.name' | t }}</label>
              <input type="text" class="input input-dark" id="contact_name" name="contact[name]" placeholder="{{ 'contact.form.name' | t }}" required/>
            </div>

            <div class="formGroup">
              <label class="label" for="contact_email">{{ 'contact.form.email' | t }}</label>
              <input type="email" class="input input-dark" id="contact_email" name="contact[email]" placeholder="{{ 'contact.form.email' | t }}" required/>
            </div>

            <div class="formGroup">
              <label class="label" for="message">{{ 'contact.form.message' | t }}</label>
              <textarea rows="6" id="message" class="input-textarea input input-dark" name="contact[body]" placeholder="{{ 'contact.form.message' | t }}" required></textarea>
            </div>

            <input class="button button-black" style="width: auto" type="submit" value="{{ 'contact.form.submit' | t }}" />
          {%- endform -%}

        </div>
      </div>

    </div>
  </div>
</section>