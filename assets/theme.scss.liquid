// Font
@font-face {
  font-family: "CalibreMedium";
  font-display: fallback;
  src: url("./Calibre-Medium.woff") format("woff");
  font-weight: normal;
  font-style: normal;
}

@font-face {
  font-family: "CalibreRegular";
  font-display: fallback;
  src: url("./Calibre-Regular.woff") format("woff");
  font-weight: normal;
  font-style: normal;
}
@font-face {
  font-family: "CalibreThin";
  font-display: fallback;
  src: url("./Calibre-Thin.woff") format("woff");
  font-weight: normal;
  font-style: normal;
}

$font-header: "CalibreMedium", sans-serif;
$font-body: "CalibreRegular", sans-serif;
$font-text: "CalibreThin", sans-serif;
// Colors
$color-white: rgba(255, 255, 255, 1);
$color-dark-grey: rgba(215, 215, 215, 1);
$color-light-grey: rgba(246, 246, 246, 1);
$color-black: rgba(0, 0, 0, 1);
$color-light-green: rgba(173, 218, 27, 1);
$color-text-grey: rgb(128, 130, 132);

// Container
$container-padding-phone: 3%;
$container-padding-tablet: 3%;
$container-padding-desktop: 3%;

// General
html,
body {
  padding: 0;
  margin: 0;
}

html {
  font-size: 62.5%;
}

body {
  background-color: $color-white;
  font-family: $font-body;
  font-size: 1.4rem;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  letter-spacing: 1px;
}
body.locked {
  overflow: hidden;
}

// Typography
h1,
h2,
h3,
h5,
h6 {
  font-family: $font-header;
  display: inline-block;
  margin: 0 0 10px;
  width: 100%;
  letter-spacing: 1px;
}

h1 {
  font-size: 5.2rem;
}

h2 {
  font-size: 4.2rem;
  font-family: $font-body;
}
h4 {
  font-family: $font-body;
  font-size: 1.4rem;
  font-weight: normal;
  letter-spacing: 1px;
}
p {
  font-family: $font-text;
  font-size: 1.6rem;
  line-height: 1.25;
  margin: 0 0 10px;
}

// Links
a,
a:active,
a:hover,
a:visited {
  text-decoration: none;
}

img,
picture {
  width: 100%;
  max-width: 100%;
  display: inline-block;
}

// Layout
.main {
}

.container {
  position: relative;
  max-width: 2000px;
  margin: 0 auto;
  width: 100%;
  box-sizing: border-box;
  padding: 0 $container-padding-phone;

  @media (min-width: 768px) {
    padding: 0 $container-padding-tablet;
  }
  @media (min-width: 1024px) {
    padding: 0 $container-padding-desktop;
  }
}
.container-full {
  max-width: 100%;
}

.section {
  position: relative;
  display: block;
  width: 100%;
  box-sizing: border-box;
}

.page {
}

.page_header {
  margin-bottom: 50px;
}

// Helpers
.text-left {
  text-align: left;
}
.text-center {
  text-align: center;
}
.text-right {
  text-align: right;
}

.hidden {
  display: none;
}

.image {
  display: inline-block;
  vertical-align: middle;
  width: 100%;
  height: auto;
}

// Button
.button {
  font-family: $font-body;
  color: $color-black;
  background: $color-white;
  display: inline-block;
  font-size: 1.6rem;
  padding: 25px;
  height: 40px;
  line-height: 0px;
  min-width: 100px;
  text-align: center;
  text-transform: capitalize;
  position: relative;
  box-sizing: border-box;
  outline: none;
  -webkit-appearance: none;
  cursor: pointer;

  &:disabled {
    cursor: not-allowed;
    opacity: 0.85;
  }
}
button:hover {
  color: $color-white;
  background: $color-black;
}
.button:hover {
  color: $color-white;
  background: $color-black;
}
.button-black {
  color: $color-white;
  background: #4c4c4b;
  text-align: center;
  width: 100%;
  border: none;
}
.button-transparent {
  color: $color-black;
  background: transparent;
  border: 1px solid rgba($color-black, 0.14);
}

// Input
.form {
  width: 100%;
}
.form-small {
  max-width: 500px;
  margin: 0 auto;
}

.formMessage {
  background-color: $color-white;
  border-radius: 20px;
  padding: 20px;
  margin-bottom: 30px;
}

.formMessage_title {
  margin-bottom: 0;
}

.formMessage .errors {
  color: red;
}

.formGroups {
  display: flex;
  flex-flow: wrap row;
  justify-content: space-between;
}

.formGroup {
  display: block;
  margin-bottom: 20px;
}
.formGroup-half {
  width: 100%;
  display: inline-block;

  @media (min-width: 768px) {
    width: calc(50% - 10px);
  }
}

.visually-hidden,
.icon__fallback-text {
  position: absolute !important;
  overflow: hidden;
  clip: rect(0 0 0 0);
  height: 1px;
  width: 1px;
  margin: -1px;
  padding: 0;
  border: 0;
}

.label {
  text-align: left;
  display: block;
  font-weight: bold;
  margin-bottom: 5px;
}

.input {
  background: transparent;
  border: 1px solid rgba($color-black, 0.5);
  border-radius: 5px;
  display: inline-block;
  font-family: inherit;
  font-size: inherit;
  font-weight: normal;
  outline: none;
  vertical-align: middle;
  width: 100%;
  padding: 0 20px;
  min-width: 100px;
  box-sizing: border-box;
  color: $color-black;

  &:focus {
    border: 1px solid $color-black;
    box-shadow: none;
    outline: none;
  }
}

.input[type="email"],
.input[type="search"],
.input[type="tel"],
.input[type="password"],
.input[type="text"] {
  box-sizing: border-box;
  height: 60px;
  line-height: 60px;
  -webkit-appearance: none;
}

.input::placeholder {
  /* Chrome, Firefox, Opera, Safari 10.1+ */
  color: $color-black;
  opacity: 1; /* Firefox */
}

.input:-ms-input-placeholder {
  /* Internet Explorer 10-11 */
  color: $color-black;
}

.input::-ms-input-placeholder {
  /* Microsoft Edge */
  color: $color-black;
}

.input-textarea,
textarea {
  box-sizing: border-box;
  padding: 10px 20px;
  -webkit-appearance: none;
  resize: none;
}

// Custom checkbox
.checkbox {
  width: 100%;
  position: relative;
  display: flex;

  input {
    background: transparent;
    background-color: transparent;
    border: 1px solid $color-black;
    border-radius: 5px;
    cursor: pointer;
    padding: 0;
    margin: 0;
    position: absolute;
    top: 0;
    left: 0;
    height: 25px;
    width: 25px;
    overflow: hidden;
    outline: none;
    display: flex;
    justify-content: center;
    align-items: center;
    -webkit-appearance: none;

    &:checked {
      background-color: $color-black;
      color: $color-white;

      &::after {
        text-transform: none;
        letter-spacing: 0;
        font-variant: initial;
        line-height: 1;
        content: "accept";
        // content: url({{ 'icon-checkmark-white.svg' | asset_url }});
      }
    }
  }

  label {
    font-size: 1.8rem;
    line-height: normal;
    text-align: left;
    margin-left: 40px;

    &:hover {
      cursor: pointer;
    }

    a {
      color: inherit;
      text-decoration: underline;
    }
  }
}

// Custom select
.select-styled {
  font-family: $font-body;
  padding: 0;
  margin: 0;
  border: 1px solid $color-black;
  min-width: 150px;
  height: 60px;
  line-height: 60px;
  border-radius: 0;
  overflow: hidden;
  background-color: transparent;
  // background: transparent url({{ 'arrowhead-down.svg' | asset_url }}) no-repeat 95% 50%;
  background-size: 15px auto;
  outline: none;

  select {
    color: inherit;
    padding: 0 20px;
    font-family: inherit;
    font-size: inherit;
    width: 100%;
    height: 100%;
    border: none;
    box-shadow: none;
    cursor: pointer;
    background-color: transparent;
    background-image: none;
    -webkit-appearance: none;
    -moz-appearance: none;
    appearance: none;
    outline: none;

    option {
      color: $color-black;
    }
  }
}

// Responsive table
table {
  margin-bottom: 20px;
  width: 100%;

  a {
    border-bottom: 1px solid currentColor;
  }
}

th {
  font-family: inherit;
  font-style: inherit;
  font-weight: bold;
}

th,
td {
  text-align: left;
  padding: 10px 14px;
}

tbody th,
tfoot th {
  font-weight: normal;
}

// Responsive tables, defined with .responsive-table on table element.
@media (max-width: 768px) {
  .responsiveTable {
    thead {
      display: none;
    }

    th,
    td {
      float: left;
      clear: left;
      width: 100%;
      text-align: right;
      padding: 5px 0;
      border: 0;
      margin: 0;
      box-sizing: border-box;
    }

    th::before,
    td::before {
      content: attr(data-label);
      float: left;
      text-align: center;
      padding-right: 10px;
      font-weight: bold;
    }
  }

  .responsiveTable_row + .responsiveTable_row,
  tfoot > .responsiveTable_row:first-child {
    position: relative;
    margin-top: 10px;
    padding-top: 40px;

    &::after {
      content: "";
      display: block;
      position: absolute;
      top: 0;
      left: 40px / 2;
      right: 40px / 2;
      border-bottom: 1px solid $color-black;
    }
  }
}

// Modal
.modal {
  background-color: rgba($color-black, 0.14);
  bottom: 0;
  left: 0;
  overflow: auto;
  position: fixed;
  right: 0;
  top: 0;
  z-index: 998;
  margin: 0;
}
.modal-small .modal_container {
  max-width: 768px;
}

.modal_container {
  background-color: $color-light-grey;
  box-sizing: border-box;
  max-width: 960px;
  width: 100%;
  margin: 40px auto;
  position: relative;
  z-index: 999;
  margin: 75px auto 75px;
  padding: 50px;
  border-radius: 5px;

  @media (min-width: 768px) {
    margin: 150px auto 75px;
  }
}

.modal_close {
  position: absolute;
  right: 25px;
  top: 25px;

  @media (min-width: 768px) {
    right: 50px;
    top: 50px;
  }
}

.modal_header {
  text-align: center;
  margin-bottom: 25px;

  @media (min-width: 768px) {
    margin-bottom: 75px;
  }
}

.modal_title {
  font-size: 2.8rem;
  font-weight: bold;
  font-stretch: normal;
  font-style: normal;
  line-height: 1.54;
  max-width: unset;

  @media (min-width: 768px) {
    font-size: 4.8rem;
    max-width: 15ch;
    line-height: 1;
  }
}

// Header
.header {
  width: 100%;
  box-sizing: border-box;
  border-bottom: 2px solid rgba(76, 76, 75, 0.1);
  z-index: 1;
  position: fixed;
  background-color: $color-white;
  top: 4rem;
}

.header_inmer {
  height: 60px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  overflow: hidden;
}
.header_inmer a {
  color: $color-text-grey;
  padding: 0 1.5rem;
  height: 100%;
}
.nav {
  height: 100%;
  align-items: center;
  margin: 0;
  width: 45%;
}
.header_logo {
  width: auto;
  max-height: 14px;
  max-width: none;
}
.logo_link {
  text-align: center;
  height: auto !important;
  width: 10%;
}
.link_title {
  padding-top: 2.3rem;
  height: 62%;
  letter-spacing: 1px;
}
.link_title:hover {
  box-shadow: inset 0 -1px 0 black;
  color: $color-black;
}
.js-cart-trigger {
  height: 100%;
  color: $color-text-grey;
}
.suitcase {
  width: 15px;
}
#suitcase_cart {
  position: relative;
  padding-top: 1.5rem;
  height: 73%;
}
.js-cart-counter {
  position: absolute;
  padding-top: 0.8rem;
  padding-left: 0.37rem;
}
#right_menu {
  justify-content: flex-end;
}
#dropdown-wrapper {
  padding-right: 3rem;
  height: 100%;
}
.menu_left_title:hover {
  box-shadow: inset 0 -1px 0 black;
  color: $color-black;
}
.menu_left_title {
  margin-top: 0.6rem;
  height: 62%;
  margin-bottom: 0;
}
.dropdown-content,
.dropdown-content-2 {
  display: none;
  position: absolute;
  background-color: #ffffff;
  width: 100vw;
  z-index: 1;
  left: 0;
  top: 100%;
  border-top: 0.2px solid #e3e3e3;
  box-shadow: inset 0 -1px 2px 1px rgba(76, 76, 75, 0.1);
}
.Shop,
.About {
  overflow: hidden;
  outline: none;
  border: none;
  margin: 0;
}
.dropdown-content a,
.dropdown-content-2 a {
  width: 25%;
  padding: 30px 15px;
}
.dropdown-content img,
.dropdown-content-2 img {
  width: 100%;
  height: 250px;
  object-fit: cover;
}
.Shop:hover .dropdown-content,
.About:hover .dropdown-content-2 {
  display: flex;
  justify-content: space-between;
}
.dropdown-content h2,
.dropdown-content-2 h2 {
  padding-top: 0.5rem;
  font-size: 1.5rem;
}
@media (max-width: 480px) {
  .header_logo {
    height: 15px;
  }
  .logo_link {
    width: 33%;
    z-index: 2;
  }
  .js-cart-trigger {
    text-align: right;
  }
  #right_menu,
  #left_menu-mobile {
    width: 33%;
    display: block !important;
  }
  #dropdown-wrapper,
  #left_menu,
  .header-faq,
  .header-signup {
    display: none !important;
  }
}
//Header Mobile

#menuToggle {
  display: block;
  position: relative;
  z-index: 1;
  -webkit-user-select: none;
  user-select: none;
}

#menuToggle a {
  text-decoration: none;
  color: #232323;
  transition: color 0.3s ease;
}

#menuToggle input {
  display: block;
  width: 40px;
  height: 32px;
  position: absolute;
  top: -7px;
  left: -5px;
  cursor: pointer;
  opacity: 0; /* hide this */
  z-index: 2; /* and place it over the hamburger */
  -webkit-touch-callout: none;
}

#menuToggle span {
  display: block;
  width: 25px;
  height: 1px;
  margin-bottom: 6px;
  position: relative;
  background: $color-black;
  z-index: 1;
  transform-origin: 4px 0px;
  transition: transform 0.5s cubic-bezier(0.77, 0.2, 0.05, 1),
    background 0.5s cubic-bezier(0.77, 0.2, 0.05, 1), opacity 0.55s ease;
}

#menuToggle span:first-child {
  transform-origin: 0% 0%;
}

#menuToggle span:nth-last-child(2) {
  transform-origin: 0% 100%;
}

#menuToggle input:checked ~ span {
  opacity: 1;
  transform: rotate(45deg) translate(-2px, -1px);
  background: #232323;
}

#menuToggle input:checked ~ span:nth-last-child(3) {
  opacity: 0;
  transform: rotate(0deg) scale(0.2, 0.2);
}

#menuToggle input:checked ~ span:nth-last-child(2) {
  transform: rotate(-45deg) translate(0, -1px);
}

#menu {
  position: fixed;
  width: 100vw;
  height: 100vh;
  //top: 20rem;
  overflow: scroll;
  margin: -100px 0 0 -50px;
  padding-top: 120px;
  padding-left: 60px;
  padding-right: 20px;
  background: $color-white;
  list-style-type: none;
  -webkit-font-smoothing: antialiased;
  transform-origin: 0% 0%;
  transform: translate(-100%, 0);
  transition: transform 0.5s cubic-bezier(0.77, 0.2, 0.05, 1);
}
#menu h3 {
  color: $color-text-grey;
  font-size: 2.5rem;
  font-family: $font-body;
  font-weight: normal;
}
#menu ul {
  list-style-type: none;
  padding: 0 45px 60px 0;
}
#menu li {
  color: $color-text-grey;
  padding: 10px 0;
  font-size: 22px;
  border-bottom: solid 0.2px #e3e3e3;
}
#menuToggle input:checked ~ #menu {
  transform: none;
}
.best-sellers {
  width: 90%;
  margin-bottom: 40px;
  padding-bottom: 80px;
  border-bottom: 0.2px solid #e3e3e3;
  justify-content: space-between;
  display: flex;
}
.best-sellers a {
  width: 48%;
  padding: 0;
}
.mobile_links {
  height: 30rem;
  width: 90%;
  column-count: 2;
}
.mobile_links a {
  font-size: 2rem;
  color: $color-text-grey !important;
  font-weight: normal;
  display: block;
  height: 40px;
  font-family: $font-body !important;
  padding: 0;
}
//Announcement Bar

// Footer
.footer {
}

// Breadcrumb
.breadcrumb {
  color: $color-black;
  font-size: 1.4rem;
  height: 40px;
  padding: 10px 0 0;
  line-height: 30px;
  text-align: left;
  text-transform: capitalize;
  font-family: $font-body;
  letter-spacing: 1px;
}

.breadcrumb a {
  color: $color-black;
  padding-right: 10px;

  &:hover {
    text-decoration: underline;
  }
}

// Product card
.productCard {
  width: 100%;
  box-sizing: border-box;
  padding: 0 0 15px 0;
  //margin: 0 10px;
}

.productCard_images {
}

.productCard_details {
}

.productCard_title {
  display: block;
  font-size: 1.3rem;
  font-weight: bold;
  margin: 10px 0 0;
  color: $color-text-grey;
  font-family: $font-body;
}

.productCard_pricing {
  color: $color-text-grey;
  font-size: 1.3rem;
  font-family: $font-body;
}

.productCard_price {
}
.productCard_price-sale {
  text-decoration: line-through;
}
// Product Page
.product {
  display: flex;
}
.product__features-left {
  width: 70%;
  display: flex;
}
.product__features-right {
  width: 30%;
  position: static;
  top: 0px;
  padding: 0 0 0 30px;
}
.product__features-right p {
  font-size: 1.5rem;
  color: $color-text-grey;
  font-family: $font-body;
}
.product__features-sticky {
  position: sticky;
  top: 20px;
}
.product__features-left ul {
  list-style: none;
  margin: 0;
  padding: 0;
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
}
.product__features-left ul li:first-child,
.product__features-left ul li:nth-child(2),
.product__features-left ul li:nth-child(3),
.product__features-left ul li:nth-child(4) {
  width: 49.5%;
}
.product__features-left ul li:nth-child(5),
.product__features-left ul li:nth-child(6),
.product__features-left ul li:nth-child(7) {
  width: 32.7%;
}
.product__header {
  display: flex;
  padding-bottom: 25px;
  justify-content: space-between;
}

.product_title,
.product_pricing span {
  font-size: 1.8rem;
  font-family: $font-header;
  font-weight: normal;
}
.product_option {
  padding: 20px 0;
  margin: 10px 0;
  border-bottom: 0.2px solid #e3e3e3;
}
// View + Shipping
.viewerShipping {
  margin: 20px 0;
  border-bottom: 0.2px solid #e3e3e3;
}
.viewerShipping h4 {
  color: $color-text-grey;
  margin: 10px 0;
}
.viewer-number,
.shipping-date,
.free-shipping {
  color: $color-black !important;
}

// Collection
.productsList {
}

.productsList_inner {
  //display: flex;
  //flex-flow: wrap row;
  //justify-content: flex-start;
  column-count: 3;
  padding: 80px 0 60px;
  border-bottom: 0.2px solid #e3e3e3;
  border-top: 0.2px solid #e3e3e3;

  @media (max-width: 768px) {
    column-count: 2;
  }
  @media (max-width: 480px) {
    column-count: 1;
  }
}
.collectionHero__overlay {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  text-align: center;
  color: white;
}
.collectionHero img {
  height: 70vh;
  object-fit: cover;
}
.collectionHero_description p {
  font-size: 2.2rem;
  font-family: $font-body;
  margin: 0;
}
@media (max-width: 480px) {
  .collectionHero img {
    max-width: 480;
    overflow: hidden;
    height: 70vh;
  }
}
// Pagination
.pagination {
  display: block;
  text-align: left;
  width: 100%;
  padding-top: 20px;
  margin-top: 40px;
  margin-bottom: 40px;

  @media (min-width: 768px) {
    text-align: center;
  }
}

.pagination_container {
  display: flex;
  justify-content: center;
  align-items: center;
}

.pagination_button {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 32px;
  height: 32px;
  background-color: $color-light-grey;
  color: $color-black;
  border: none;
  border-radius: 50%;
  margin: 0 5px;
  -webkit-appearance: none;
  outline: none;
  cursor: pointer;
  order: 2;

  &:disabled {
    opacity: 0.85;
    cursor: not-allowed;
  }
}

.pagination_button-prev {
  &::before {
    content: "<";
    width: 24px;
    height: 24px;
  }
}

.pagination_button-next {
  &::before {
    content: ">";
    width: 24px;
    height: 24px;
  }
}

.pagination_numbers {
  margin: 0;
  display: block;
  order: 1;
  width: 100%;

  li {
    list-style: none;
    display: inline-block;
  }

  @media (min-width: 768px) {
    margin: 0 35px;
    order: 2;
    width: auto;
  }
}

.pagination_number {
  font-size: 1.8rem;
  color: $color-black;
  opacity: 0.5;
  margin: 0 8.5px;
  transition: opacity 200ms ease-in-out;

  &:hover {
    opacity: 1;
  }

  a {
    font-size: inherit;
    font-weight: 300;
  }
}
.pagination_number-current {
  opacity: 1;

  a {
    cursor: default;
    font-weight: bold;
  }
}
/*------------- Landing Page --------------*/

// Hero Banner
.heroBanner {
  position: relative;
  background-color: white;
}
.heroBanner_overlay {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  text-align: center;
  color: white;
  //z-index: 1;
}
.header-img {
  width: 100%;
}

@media (max-width: 480px) {
  .heroBanner video {
    height: 85vh;
    object-fit: cover;
  }
}
// Travel Essentials
.travelEssentials {
  display: inline-flex;
  align-items: flex-end;
  padding-top: 50px;
  padding-bottom: 50px;
}
.essentials {
  width: 60%;
}
.essentials__heading {
  color: rgb(128, 130, 132);
  padding: 50px 0;
}
.travelEssentials h1 {
  font-size: 2rem;
  color: $color-black;
  font-family: $font-body;
}
.travelEssentials p {
  font-size: 1.2rem;
}
.essentials__small {
  display: flex;
  justify-content: space-between;
}
.essentials__small img {
  width: 95%;
}
.essentials__big {
  width: 40%;
}
@media (max-width: 480px) {
  .travelEssentials {
    display: block;
    padding-bottom: 0px;
    padding-top: 0px;
  }
  .essentials__heading {
    padding: 25px 0;
  }
  .essentials {
    width: 100%;
  }
  .essentials__small a {
    width: 48.5%;
  }
  .essentials__small img {
    width: 100%;
  }
  .essentials__big {
    width: 100%;
    padding: 3% 0;
  }
}

// Product Highlights

// Sustainable Design
.sustainableDesign {
  display: flex;
  background-color: white;
  justify-content: space-between;
}
.sustainableDesign img {
  object-fit: cover;
  width: 49.7%;
  height: 85vh;
}
.sustainableDesign__overlay {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  text-align: center;
  color: white;
}
.sustainableDesign__overlay h2 {
  font-size: 3rem;
}
.sustainableDesign__img {
  display: flex;
}
@media (max-width: 480px) {
  .sustainableDesign__img-2 {
    display: none;
  }
  .sustainableDesign img {
    object-fit: cover;
    width: 100%;
    height: 60vh;
  }
}

// Explore More
.exploreMore {
  padding-top: 50px;
  padding-bottom: 50px;
  background-color: white;
}
.exploreMore h1 {
  color: #808284;
  font-size: 2rem;
  letter-spacing: 1px;
}
.explore__flex {
  display: flex;
  justify-content: space-between;
}
.explore__blocks {
  width: 24.5%;
}
.explore__blocks a {
  color: black;
  text-align: left;
}
.explore__blocks h2 {
  color: #808284;
  font-size: 1.2rem;
  letter-spacing: 1px;
  padding: 10px 0;
}
@media (max-width: 480px) {
  .explore__flex {
    display: grid;
    grid-template-columns: 1fr 1fr;
    grid-column-gap: 3%;
  }
  .explore__blocks {
    width: 100%;
  }
}

//Collection Highlights
.collectionHighlights {
  padding-top: 70px;
  padding-bottom: 30px;
}
.collectionHighlights__flex {
  display: inline-flex;
  justify-content: space-between;
}
.collectionHighlights__highlights {
  width: 49.7%;
  position: relative;
  height: 90vh;
}
.collectionHighlights__highlights img {
  height: 100%;
  object-fit: cover;
  object-position: 10% 0%;
}
.collectionHighlights__overlay {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  text-align: center;
  color: white;
}
.collectionHighlights__highlights h2 {
  font-size: 3rem;
  letter-spacing: 1px;
}
@media (max-width: 480px) {
  .collectionHighlights__flex {
    display: inline-block;
  }
  .collectionHighlights__highlights {
    width: 100%;
    height: auto;
  }
}
// Personaliser
#pplr-4763492024455 {
  display: none !important;
}
