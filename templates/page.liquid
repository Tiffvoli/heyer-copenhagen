{% assign page_title = page.title %}
{% assign page_content = page.content %}

<section class="page section">
  <div class="container">
    <header class="page_header">
      <h1 class="page_title">{{ page_title }}</h1>
    </header>
    <div class="page_content">{{ page_content }}</div>
  </div>
</section>
